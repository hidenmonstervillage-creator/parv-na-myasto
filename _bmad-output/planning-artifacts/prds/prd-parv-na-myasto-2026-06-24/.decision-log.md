# Decision Log — PRD: Пръв на Място (2026-06-24)

Canonical memory + audit trail for this PRD run.

## Decisions

- **2026-06-24 — Intent: Create (fresh run).** Prior run `prd-parv-na-myasto-2026-06-21` was an empty stub (`status: draft`, no content); started a new run for today instead of resuming. Brief `brief-parv-na-myasto-2026-06-19` reused as foundational input.
- **2026-06-24 — Scope: Hybrid.** PRD frames the real problem + full product vision as context, but scopes the actual MVP requirements (FR/NFR) to the simplified Spring Boot demo that builds and runs. Commercial vision (subscriptions, fleet, mobile PWA) goes in a "Future / Vision" section, not the MVP requirements.
- **2026-06-24 — Working mode: Fast path.** Facilitator drafts the full PRD with `[ASSUMPTION]` tags; user reviews and iterates.
- **2026-06-24 — Language: English** (matches config `communication_language` / `document_output_language`).
- **2026-06-24 — Architecture section: yes.** User confirmed including an "Architecture & Key Decisions" section in the PRD body (not just the addendum) as the primary signal for a dev-role reader. PRD §10.
- **2026-06-24 — Pricing: light/qualitative.** Commercial tiers kept illustrative/qualitative in §12 Future/Vision, tagged `[ASSUMPTION]`; not in MVP requirements.
- **2026-06-24 — Traction: none assumed.** No real pilot/operator interest claimed; treated as portfolio/learning project (tagged, §8 Q4).
- **2026-06-24 — Maven Wrapper added** to `backend/` (mvnw/mvnw.cmd/.mvn) so the project runs without a Maven install; docs + PRD (§2 UJ-2, §6, §10) updated to `./mvnw spring-boot:run`.
- **2026-06-24 — Cleanup:** deleted empty stub run `prd-parv-na-myasto-2026-06-21/`; restored a truncated sentence in PRD §2.3 UJ-1 path.
- **2026-06-24 — Demo Seed removed (no fabricated data).** User decision: the product must never show fake incidents. Removed `DemoSeeder` from the code (delegated to a developer subagent) and replaced the concept everywhere with an honest **Empty State**: when an Operator has no incidents in range, the dashboard says so. PRD changes: dropped old FR-3 (Demo seed), renumbered FR-4→FR-3 … FR-8→FR-7, added new **FR-8 (Honest empty state — no fabricated data)** under §4.3; updated Glossary (Demo Seed → Empty State), UJ-1 edge case, §6.1 scope, SM-1/SM-2/SM-3 validations, §11 reliability NFR; updated addendum + root/back-end READMEs. Operators remain seeded (no signup yet); incidents are always real.
- **2026-06-24 — Data source: Waze → TomTom.** Waze's livemap hard-blocks automated requests (403, reproduced with full browser headers; `rtserver` endpoints 404). Switched to the **TomTom Traffic Incident Details API** (sanctioned, free key, reachable — returns clean 401 without key). Introduced an `IncidentSource` interface; `TomTomClient` implements it (maps iconCategory → ACCIDENT/HAZARD, parses Point/LineString geometry); removed `WazeClient`/`WazeProperties`; rewrote `IncidentPoller` to the seam. Key via `TOMTOM_API_KEY`; without it the app runs and shows the honest empty state. Compiled + boots; no-key warning verified. Docs (PRD §3/§4/§6/§8/§9/§10/§12, addendum, READMEs) updated.
- **2026-06-24 — Persistence: optional Supabase Postgres.** User asked to connect Supabase (reverses the earlier in-memory-only simplification). Scope chosen: persist incidents + operators via JDBC; matching stays in Java. Made `IncidentStore`/`OperatorStore` interfaces with `InMemory*` (default) and `Jdbc*` (`app.storage=supabase`) implementations; shared `DefaultOperators` seed; `SupabaseConfig` provides the only `DataSource` (Hikari), with `DataSourceAutoConfiguration` excluded so the in-memory build needs no DB. Tables auto-created + operators seeded on first run. Compiled; memory mode verified (6 operators, no bean errors). Supabase mode needs the user's connection string to runtime-verify. PRD §6/§10/§11 + addendum + READMEs updated.
- **2026-06-24 — Draft v1 written** (Fast path). prd.md (§0–§12) + addendum.md (provenance, rejected alternatives, scale path) created. Awaiting user review.
- **2026-06-24 — Context:** Codebase was radically simplified this session (removed Supabase/Postgres/PostGIS, Deno edge functions, React/Vite frontend, auth/subscriptions) into one self-contained Spring Boot app: live Waze poll → in-memory store → haversine `GeoMatcher` → SSE push → static Leaflet UI. Verified compiling + running; per-operator proximity match confirmed correct. Live Waze returns 403 (bot-blocked); `DemoSeeder` seeds sample incidents so the demo always shows matches.
