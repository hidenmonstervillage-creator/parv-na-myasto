# Decision Log — Brief: Пръв на Място

## 2026-06-19

- **Intent:** Create.
- **Purpose chosen:** Customer / partner pitch (audience = tow operators & fleet managers). → drives every section toward operator pain, payoff, and price.
- **Working mode:** Fast path — draft full brief with `[ASSUMPTION]` tags, user corrects in review.
- **Source of facts:** Existing codebase (schema, edge functions, frontend), README, and `docs/architecture.md` (authored same day by architect agent). No project-context.md exists.
- **Confirmed from code (not assumptions):**
  - Mobile-first PWA; GPS + push notifications.
  - Target users: tow-truck operators in Bulgaria.
  - Mechanism: poll live incident data → PostGIS zone match (base + radius_km, default 15) → realtime push with one-tap nav (Waze/Google).
  - Business model: tiered subscription `trial / starter / pro / fleet / expired` with free trial.
  - Funnel instrumentation exists: notifications track `seen_at` and `converted`.
- **Key open assumptions flagged in brief (need user input):** real status-quo job-finding behavior + cost of being second; real tier pricing & tier differentiators; beachhead segment (independents vs fleets) + Bulgarian market size; traction/proof; one-line hook.
- **User instruction:** Restarted the round — declined the consolidated Q&A, asked to "begin from the start" and just create the brief md file directly. Honored: drafted full brief now with assumptions tagged rather than waiting on answers.

## 2026-06-21

- **Stack change (supersedes the backend facts above):** the backend was re-platformed from
  Supabase Edge Functions (Deno/TypeScript) to a **Java Spring Boot** service under `backend/`.
  The database tier — Supabase Postgres + PostGIS + Realtime + Auth — and the React PWA are
  unchanged. The "edge functions" referenced in the 2026-06-19 source-of-facts note now live as
  Spring components (`IncidentPoller`, `IncidentNotifier`, `OperatorController`); the legacy
  `supabase/functions/` are kept for reference until the Spring backend is verified on live data.
  The PostGIS zone-match mechanism and the subscription/funnel facts are unaffected.
  See `docs/architecture.md` (revised 2026-06-21).
